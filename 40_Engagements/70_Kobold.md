## Privilege escalation

```
Ubuntu 24.04.4 LTS
```

process run by root
`/usr/bin/docker-proxy -proto tcp -host-ip 127.0.0.1 -host-port 8080 -container-ip 172.17.0.2 -container-port 8080 -use-l
isten-fd`

$WorkDirectory /var/spool/rsyslog                                                                                                                                      

$IncludeConfig /etc/rsyslog.d/*.conf